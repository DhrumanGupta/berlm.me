function Footer() {
  return (
    <footer className="mt-6 py-4">
      <p className="text-sm text-gray-600 dark:text-gray-400">
        {new Date().getFullYear()} — @dhrumangupta
      </p>
    </footer>
  );
}

export default Footer;
